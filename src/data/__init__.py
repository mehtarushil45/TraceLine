"""Data layer for TraceLine: raw-data loading and synthetic enrichment.

Submodules (:mod:`src.data.entities`, :mod:`src.data.enrichment`) are imported
lazily by consumers so ``python -m src.data.enrichment`` stays warning-free.
"""
